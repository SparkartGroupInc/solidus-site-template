{
  "name": "{%= name %}",
  "version": "0.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "build": "gulp build",
    "watch": "gulp watch",
    "preinstall": "npm config set //registry.npmjs.org/:_authToken $NPM_TOKEN",
    "predeploy": "npm run build && gulp predeploy",
    "deploy": "deploy --options-from=./node_modules/@sparkart/broadway/config/deploy-modulus-production",
    "predeploy-review": "npm run build",
    "deploy-review": "deploy-review --options-from=./node_modules/@sparkart/broadway/config/deploy-modulus-review",
    "test": "echo"
  },
  "devDependencies": {
    "@sparkart/broadway": "^3.0.0"
  },
  "dependencies": {
    "js-yaml": "^3.2.3",
    "solidus": "^2.1.2",
    "solidus-metadata": "^1.0.1"
  },
  "engines": {
    "node": "0.12.7",
    "npm": "^2.0.0"
  }
}
